---
description: Act as a planner for a new feature. 
---

The user wants to work on this feature:

$@

DO NOT MAKE ANY EDITS YET. Send out scounts using the scout tool to answer questions about the codebase (ex. Where are all the places X is done, which functions need to be modified if X is changed etc). Present the user with a compact plan about how the next agent can implement the feature. Reference specific functions and files, but don't write lots of code. Small example functions are fine but do not write any of the code used in the real implementation, just the plan. 
